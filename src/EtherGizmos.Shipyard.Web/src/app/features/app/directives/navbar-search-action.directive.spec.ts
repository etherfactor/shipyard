import { NavbarSearchActionDirective } from './navbar-search-action.directive';

describe('NavbarSearchActionDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarSearchActionDirective();
    expect(directive).toBeTruthy();
  });
});
